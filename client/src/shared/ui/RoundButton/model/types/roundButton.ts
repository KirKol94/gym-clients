export enum RoundButtonSize {
  S = 's',
  M = 'm',
}

export enum RoundButtonDirection {
  UP = 'up',
  RIGHT = 'right',
  DOWN = 'down',
  LEFT = 'left',
}

export enum RoundButtonTheme {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
}
